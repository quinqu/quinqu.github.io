# luminouscells.com

Personal site for Quin Quintero, served by GitHub Pages (custom domain via `CNAME`).

Plain static HTML with no build step: edit `index.html`, `experience.html`, or
`music.html` directly and push to `main`.
